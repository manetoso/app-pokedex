import { DetailPokemonData } from '../../../interfaces/pokemonData';

interface Props {
  isLg: Boolean;
  pokemonData: DetailPokemonData;
}

export const PokemonCardModal = ({ isLg, pokemonData }: Props) => {
  return <></>;
};
